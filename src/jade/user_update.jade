| {% extends 'base.html' %}
|{% load bootstrap %}

| {% block title %}Редактирование профиля{% endblock %}
| {% block headers %}
| {{ form.media }}
| {% endblock %}
| {% block content %}
.row
  .col-md-6.col-md-offset-3
    .panel.panel-default
      .panel-body
        h1 Редактирование профиля
        br
        | {% if messages %}
        | {% for message in messages %}
        .alert(class="alert-{{ message.tags }}") {{ message }}
        | {% endfor %}
        | {% endif %}
        form(method="post" action="{% url 'user_update' %}" enctype='multipart/form-data')
          | {% csrf_token %}
          |{{ form|bootstrap }}
          button.btn.btn-primary(type="submit") Сохранить
        br
        a.btn.btn-warning(href="#") Изменить пароль
| {% endblock %}